import { SoonPlaceholder } from "@/components/timeline/SoonPlaceholder";

export const metadata = { title: "食光 · 集" };

export default function CollectionsPage() {
  return (
    <SoonPlaceholder
      eyebrow="集"
      title="把吃过的，收成一本一本"
      body="主题集：爸妈来的那一周、在家做饭、出差时的那些一顿。"
    />
  );
}
